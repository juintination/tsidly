server:
  port: 8082

spring:
  application:
    name: url-redirect-service

  config:
    import: file:.env.${spring.profiles.active}[.properties]

  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}

endpoints:
  url-shortener-service:
    url: ${SHORTENER_SERVICE_URL}

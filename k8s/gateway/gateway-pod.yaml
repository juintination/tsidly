apiVersion: v1
kind: Pod
metadata:
  name: gateway-pod
spec:
  containers:
    - name: gateway
      image: tsidly-gateway
      imagePullPolicy: IfNotPresent

      envFrom:
        - configMapRef:
            name: gateway-config

      ports:
        - containerPort: 8080
